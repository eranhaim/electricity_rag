services:
  electricity-rag:
    build: .
    container_name: electricity-rag
    restart: unless-stopped
    ports:
      - "8085:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
